@model DoctorServiceModel

<div class="col-md-4">
    <div class="card mb-3">
        <img class="card-img-top" src="~/img/profiles/@Model.ProfilePictureFilename" alt="House Image">
        <div class="card-body text-center">
            <h4>@Model.Fullname</h4>

            <h6>Office number: <b>@Model.OfficePhoneNumber</b></h6>
            <h6>
                Price Per Month:
                <b>@String.Format("{0:f2}", Model.PricePerHour) &euro;</b>
            </h6>
            <h6>
                Speciality:
                <b>@Model.Speciality</b>
            </h6>
            <br />
            <a asp-controller="House" asp-action="Details" asp-route-id="@Model.Id"
               class="btn btn-success">Details</a>
            @if (this.User?.Identity?.IsAuthenticated ?? false)
            {
                <a asp-controller="House" asp-action="Edit" asp-route-id="@Model.Id"
                   class="btn btn-warning">Book</a>

                <p></p>

            }
        </div>
    </div>
</div>
